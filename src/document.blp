using Gtk 4.0;
using Adw 1;

template $Document : Gtk.Box {
      halign: fill;

      Box cells {
        spacing: 5;
        hexpand: true;
        orientation: vertical;
        homogeneous: false;
      }
}